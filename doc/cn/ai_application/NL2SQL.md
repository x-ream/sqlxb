# NL2SQL 实验

本文档是 `xb/doc/ai_application/NL2SQL.md` 的中文版本。它跟踪将自然语言转换为 xb 构建器的实验。

---

## 方法

- 使用 LLM 生成结构化意图（过滤器、排序、限制）。
- 在将意图应用于构建器之前验证意图。
- 为不支持的查询提供回退模板。

---

## 安全性

- 白名单允许的列以避免任意 SQL 注入。
- 要求显式的租户/用户过滤器。
- 记录自然查询和渲染的 SQL 以进行审计。

---

## 参考资料

- `doc/cn/AI_APPLICATION.md`

