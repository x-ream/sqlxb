# AI 应用快速开始

本文档是 `xb/doc/ai_application/QUICKSTART.md` 的中文版本。它引导你将 xb 集成到 AI 堆栈中（嵌入 → 向量数据库 → LLM）。

---

## 管道

1. 分块并嵌入文档。
2. 存储向量 + 元数据。
3. 使用 xb 查询 SQL/向量源。
4. 将结果提供给你的 LLM（RAG、agent、分析）。

有关涵盖每个步骤的代码片段，请参阅 `doc/cn/AI_APPLICATION.md`。

