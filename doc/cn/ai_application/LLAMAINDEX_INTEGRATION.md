# LlamaIndex 集成

本文档是 `xb/doc/ai_application/LLAMAINDEX_INTEGRATION.md` 的中文版本。

---

## 技巧

- 实现一个调用 xb 并将结果提供给 LlamaIndex 节点的自定义检索器。
- 在 xb 中使用元数据过滤器以镜像 LlamaIndex 元数据查询。
- 如果你的 LlamaIndex 图频繁重用它们，则缓存 xb 负载。

有关端到端示例，请参阅 `doc/cn/AI_APPLICATION.md`。

