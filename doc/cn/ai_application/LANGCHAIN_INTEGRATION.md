# LangChain 集成

本文档是 `xb/doc/ai_application/LANGCHAIN_INTEGRATION.md` 的中文版本。

---

## 用法

- 在 LangChain 工具或检索器中包装 xb 查询。
- 直接从 `JsonOfSelect()` 返回 Qdrant JSON 负载。
- 在执行前将 LangChain 过滤器映射到 xb 构建器调用。

示例伪代码可在 `doc/cn/AI_APPLICATION.md` 中找到。

