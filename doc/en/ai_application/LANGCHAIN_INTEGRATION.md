# LangChain Integration (English)

English summary of `xb/doc/ai_application/LANGCHAIN_INTEGRATION.md`.

---

## Usage

- Wrap xb queries inside LangChain tools or retrievers.
- Return Qdrant JSON payloads directly from `JsonOfSelect()`.
- Map LangChain filters to xb builder calls before execution.

Example pseudo-code is available in `doc/en/AI_APPLICATION.md`.


